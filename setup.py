from setuptools import find_packages, setup

setup(
    name="bpcl",
    version="0.1.0",
    description="System and network status utilities",
    packages=find_packages(),
    python_requires=">=3.8",
)
